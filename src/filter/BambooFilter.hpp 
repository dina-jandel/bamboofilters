#ifndef BAMBOO_FILTER_HPP
#define BAMBOO_FILTER_HPP

#include <vector>
#include <string>

class BambooFilter {
public:
    BambooFilter(size_t size, size_t hashCount);

    void insert(const std::string& item);

    bool contains(const std::string& item) const;

private:
    size_t size;
    size_t hashCount;

    std::vector<bool> table;

    size_t hash(const std::string& s, size_t seed) const;
};

#endif