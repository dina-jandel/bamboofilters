// this code was written by team member: Marija Špoljarić
#pragma once

#include <string>

// function that reads a fasta file and returns dna sequence as a string
std::string readFasta(const std::string &filename);